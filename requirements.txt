# Build marker 2026-06-15b — full ArcGIS install (ends the minimal-deps missing-module loop). Forces a fresh commit/redeploy.
Flask>=3.0,<4.0
gunicorn>=21.2
cryptography>=42.0
python-pptx>=0.6.23

# FULL ArcGIS install. We previously used a --no-deps "minimal" install to save
# memory, but it kept surfacing undocumented missing modules (truststore,
# cachetools, puremagic, ...). The full install pulls everything correctly and
# still fits Render Starter's 512 MB for sign-in and typical decks.
arcgis>=2.3,<2.5
