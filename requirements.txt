# Build marker 2026-06-15e — adds public Google Slides link import. Forces a fresh commit/redeploy.
Flask>=3.0,<4.0
gunicorn>=21.2
cryptography>=42.0
python-pptx>=0.6.23
requests>=2.31

# SmartArt rendering: crops LibreOffice-rendered slide pages. LibreOffice itself
# is a SYSTEM package (see Dockerfile), not pip. If absent, SmartArt is skipped.
pymupdf>=1.24

# Full ArcGIS install (pulls numpy/pandas etc. automatically).
arcgis>=2.3,<2.5
